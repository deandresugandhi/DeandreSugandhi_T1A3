import json

def access_win_history(username):
    with open("users.json", "r") as file:
        users = json.load(file)
    users = 

class PlayerLounge:
    def __init__(self, high_score_board, player_color):
        self.high_score_board = high_score_board